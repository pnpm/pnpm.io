---
id: install-test
title: pnpm install-test
---

Aliases: `it`

Executa `pnpm install` seguido imediatamente por `pnpm test`. Recebe exatamente os mesmos argumentos que [`pnpm install`](./install.md).
