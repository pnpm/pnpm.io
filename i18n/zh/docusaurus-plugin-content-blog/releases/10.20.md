---
title: pnpm 10.20
authors: zkochan
tags: [ release ]
date: 2025-10-28
---

此版本为 `pnpm help` 命令添加了 `--all` 标志，用于打印所有命令。

<!-- truncate -->

### 次要更改

#### `pnpm help --all`

在 `pnpm help` 中添加了对 `--all` 选项的支持，以列出所有命令 [#8628](https://github.com/pnpm/pnpm/pull/8628)。

### 补丁更改

- 当 `latest` 版本不满足 `minimumReleaseAge` 配置的成熟度要求时，选择足够成熟的最高版本，即使它具有不同的主版本号 [#10100](https://github.com/pnpm/pnpm/issues/10100)。
- `create` 命令不应该验证补丁信息。
- 切换到不同版本的 pnpm CLI 时，将 `managePackageManagerVersions` 设置为 `false`，以避免后续切换 [#10063]（https://github.com/pnpm/pnpm/issues/10063)。
