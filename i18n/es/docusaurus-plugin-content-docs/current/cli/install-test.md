---
id: install-test
title: pnpm install-test
---

Alias: `it`

Ejecuta `pnpm install` seguido inmediatamente por `pnpm test`. Tomará exactamente los mismos argumentos que [`pnpm install`](./install.md).
