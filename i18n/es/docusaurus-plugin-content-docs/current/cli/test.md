---
id: test
title: pnpm test
---

Alias: `run test`, `t`, `tst`

Ejecuta un comando arbitrario especificado en la propiedad `test` del objeto `scripts` en el package. json.

El uso previsto de esta propiedad es especificar un comando que ejecute pruebas unitarias o de integración para tu programa.
