---
id: test
title: pnpm test
---

Aliases: `run test`, `t`, `tst`

Runs an arbitrary command specified in the package's `test` property of its
`scripts` object.

El uso previsto de esta propiedad es especificar un comando que ejecute pruebas unitarias o de integración para tu programa.
